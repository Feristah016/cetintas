CREATE TABLE calisanlar (
    id INT,
    ad VARCHAR(50),
    soyad VARCHAR(50),
    yas INT,
    birim VARCHAR(50),
    maas DECIMAL(10, 2)
);

CREATE TABLE mudur (
    id INT,
    kullanici_adi VARCHAR(50),
    sifre VARCHAR(50)
);
